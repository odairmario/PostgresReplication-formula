# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkgdepedencies:
    - gnupg
    - wget
    - lsb-release
  pkg:
    version: "14"
  rootgroup: root
  config: "/etc/postgresql/"
  service:
    name: postgresql
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
